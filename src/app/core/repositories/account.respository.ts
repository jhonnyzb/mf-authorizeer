import { Observable } from "rxjs";

export abstract class AccountRepository{
  // abstract getAccount(accountId: number): Observable<any>;
  // abstract getTransactions(programId: number): Observable<any>;
}
